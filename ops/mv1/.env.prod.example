# ================================================================
# MV1 — .env de producción
# Copiar como .env y completar los valores reales
# ================================================================

# Tu usuario de Docker Hub (ej: aracely25)
DOCKERHUB_USERNAME=ssilvar2005

# IP privada de MV-DB (ver EC2 console → Private IPv4)
DB_HOST=10.0.1.50

# Secretos JWT — deben ser idénticos en MV1 y MV2
JWT_SECRET=utec_rate_secret_2025
JWT_REFRESH_SECRET=utec_rate_refresh_2025
